const chai = require("chai");
const expect = chai.expect;

describe(`@JAT tests`, function() {
  it("@test1 - mapping of scalars (string, boolean and number)", () => {
    throw new Error("NOP");
  });
  it("@test2 - mapping of an array", () => {
    throw new Error("NOP");
  });
  it("@test3 - mapping of an object", () => {
    throw new Error("NOP");
  });
  it("@test4 - mapping with array_trasform", () => {
    throw new Error("NOP");
  });
});
